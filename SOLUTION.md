# Hai提词器解决方案

## 用户问题

电脑端口播和演示场景里，用户通常同时面对三项任务：看镜头、组织表达、控制讲稿。传统按速度滚动的提词器把“控制讲稿”这件事留给用户，容易造成视线游移、语速不自然和表达中断。

## 解决思路

Hai提词器以朗读内容作为推进信号，而不是以时间作为推进信号。

```text
用户朗读
   ↓
macOS 语音识别
   ↓
按顺序匹配讲稿中的词
   ↓
更新已读进度
   ↓
把当前内容滚动到第二行附近
```

如果当前语音无法在附近内容中可靠匹配，系统才降低顺序优先级，尝试从讲稿其他位置重新定位。这种策略用于减少同词重复、语音误识别造成的进度乱跳。

## 产品原则

### 1. 表达优先

用户应该按自己的节奏说话，提词器负责跟随。系统不要求用户适应固定滚动速度。

### 2. 低干扰

界面只保留准备讲稿、开始跟读和调整提词窗所需的核心功能。使用过程中，主要注意力应留在镜头和表达上。

### 3. 本地优先

讲稿与历史记录保存在用户自己的 Mac，不建立账号体系，不搭建稿件云端服务。

### 4. 边界透明

全屏演示置顶、录屏隐藏和系统语音识别都受 macOS 与第三方软件限制。产品会持续优化兼容性，但不会把系统限制包装成绝对承诺。

## 核心模块

| 模块 | 用户价值 | 当前状态 |
| --- | --- | --- |
| 稿件编辑 | 快速粘贴和修改讲稿 | 已完成 |
| 悬浮提词窗 | 靠近镜头查看讲稿 | 已完成 |
| AI 语音跟读 | 按真实朗读进度推进 | 已完成，持续优化 |
| 自动滚动 | 当前内容保持在第二行附近 | 已完成 |
| 重新定位 | 从中间开始后恢复进度 | 已实现，稳定性优化中 |
| 显示调节 | 调节字号、透明度和窗口尺寸 | 已完成 |
| 录屏隐藏 | 尝试让提词窗不进入录制画面 | 已完成，依赖系统与录屏软件 |
| 历史文本 | 恢复最近使用的讲稿 | 已完成 |
| 演示置顶 | PPT 播放时保持可见 | 持续优化 |

## MVP 验证重点

当前版本主要验证四件事：

1. 用户是否愿意用语音跟读代替固定速度滚动。
2. 按词推进是否比按句推进更自然。
3. 把当前内容保持在第二行是否能减少视线移动。
4. 悬浮、录屏隐藏和历史文本是否覆盖真实录制工作流。

## 后续演进

短期重点是稳定性和分发体验，而不是堆叠功能。优先顺序为：语音跟读稳定性、演示场景兼容、正式签名公证、稿件管理，最后再考虑云同步和跨设备能力。
